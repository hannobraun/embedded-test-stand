name: Rust

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build firmware
      run: |
        cd test-firmware
        cargo build --verbose
    - name: Build test suite
      run: |
        cd test-suite
        cargo build --verbose
